function isPalindrome(name) {
    for (let i = name.length - 1; i >= 0; i--) {
        console.log(name[i])
    } if (name.isPalindrome) {
        console.log("it is a palindrome")
    } else {
        console.log("it is not a palindrome")
    }
}
isPalindrome("pop")
isPalindrome("shashank")
